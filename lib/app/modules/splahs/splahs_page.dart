import 'package:flutter/material.dart';

class SplahsPage extends StatelessWidget {

  const SplahsPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Splahs Page'),),
           body: Container(),
       );
  }
}